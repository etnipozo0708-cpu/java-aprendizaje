
# 📈 Bitácora de Progreso

Registro de lo que voy aprendiendo día a día en Java.
---
---
---

## 📅 Sesión 4 — Condicionales

**Fecha:** (***)

### Lo que hice:
- Creé el programa `Condicionales.java`
- Solicité la edad del usuario por teclado
- Implementé una estructura `if - else` para determinar si es mayor o menor de edad

### Lo que aprendí:
- Estructura de control `if` para evaluar una condición
- Uso de `else` para ejecutar un bloque alternativo
- Operadores de comparación dentro de un `if` (en este caso `>=`)
- Cómo anidar la lógica de decisión dentro del flujo del programa

### Próximo paso:
- Programa `Bucles.java`
- Conceptos: bucles `for`, `while` y `do-while`

### Dudas pendientes:
- (ninguna por ahora)

---

## 📅 Sesión 3 — Entrada de Usuario (Scanner)

**Fecha:** (***)

### Lo que hice:
- Creé el programa `EntradaUsuario.java`
- Leí datos por teclado usando la clase `Scanner`
- Solicité al usuario su nombre, edad y estatura
- Calculé el año de nacimiento a partir de la edad actual

### Lo que aprendí:
- Cómo importar y crear un objeto `Scanner` para leer datos
- Métodos de lectura: `nextLine()` para texto, `nextInt()` para enteros, `nextDouble()` para decimales
- Cómo concatenar variables de diferentes tipos en un mensaje de salida
- Realizar cálculos con los datos ingresados por el usuario

### Próximo paso:
- Programa `Condicionales.java`
- Conceptos: estructuras de control `if`, `else if`, `else` y `switch`

### Dudas pendientes:
- (ninguna por ahora)

---

## 📅 Sesión 2 — Operadores

**Fecha:** (***)

### Lo que hice:
- Creé el programa `Operadores.java`
- Realicé operaciones aritméticas básicas (suma, resta, multiplicación, división y módulo)
- Practiqué operadores de comparación y lógicos

### Lo que aprendí:
- Operadores aritméticos: `+`, `-`, `*`, `/`, `%`
- Operadores de comparación: `>`, `<`, `==`, `!=`, `>=`, `<=`
- Operadores lógicos: `&&` (AND), `||` (OR), `!` (NOT)
- La diferencia entre asignación (`=`) y comparación (`==`)

### Próximo paso:
- Programa `Condicionales.java`
- Conceptos: estructuras de control `if`, `else if`, `else` y `switch`

### Dudas pendientes:
- (ninguna por ahora)

---


## 🗓️ Sesión 1 — Configuración del entorno

**Fecha:** (13 de septiembre del 2026)

### Lo que hice:
- Instalé JDK 21 LTS (Eclipse Temurin)
- Configuré VS Code con Extension Pack for Java
- Instalé y configuré Git
- Creé cuenta en GitHub: `etnipozo0708-cpu`
- Creé repositorio: `java-aprendizaje`
- Creé el programa `HolaMundo.java`
- Creé el `README.md` del repositorio

### Lo que aprendí:
- Estructura básica de un programa Java (`public class`, `main`, `System.out.println`)
- Cómo funciona Git: `git add`, `git commit`, `git push`
- Cómo hacer un README en Markdown

### Próximo paso:
- Programa `VariablesYTipos.java`
- Conceptos: variables, tipos de datos (`int`, `double`, `String`, `boolean`, `char`)

### Dudas pendientes:
- (ninguna por ahora)

---